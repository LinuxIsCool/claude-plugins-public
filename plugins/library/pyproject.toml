[project]
name = "library-plugin"
version = "0.1.0"
description = "Universal resource library plugin for Claude Code"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "numpy>=1.26.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]

[tool.uv]
dev-dependencies = ["pytest", "pytest-asyncio"]
