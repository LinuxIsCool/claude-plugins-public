Read:
- @README.md


Use git as an agent to agent communication mechanism. 
Leave extensive metadata in your commit messages including title, description, summary, systems, lessons, tips, playbooks, entities, relationships.

Use feature branches that make PRs to Dev and Dev uses PRs to Main.

Main is Deployed.

Plug is installed using Plug.
